// Firebase project configuration.
//
// This file is safe to commit — a Firebase web config is a public client
// identifier, not a secret. Real access control lives in firestore.rules
// and in the Auth providers you enable, not in hiding these values.
//
// Replace the placeholders below with the config from:
//   Firebase Console → Project settings → General → Your apps → SDK setup and configuration
// See README.md for the full setup walkthrough.
export const firebaseConfig = {
  apiKey: "REPLACE_WITH_YOUR_API_KEY",
  authDomain: "REPLACE_WITH_YOUR_PROJECT.firebaseapp.com",
  projectId: "REPLACE_WITH_YOUR_PROJECT_ID",
  storageBucket: "REPLACE_WITH_YOUR_PROJECT.appspot.com",
  messagingSenderId: "REPLACE_WITH_YOUR_SENDER_ID",
  appId: "REPLACE_WITH_YOUR_APP_ID"
};
